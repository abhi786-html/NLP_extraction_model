# NLP_extraction_model

# IMDB Sentiment Analysis Project

## 📌 Project Overview
This project performs **Sentiment Analysis** on the **IMDB Movie Reviews** dataset using **Machine Learning Models** such as:
- **Logistic Regression**
- **Naive Bayes**
- **Support Vector Machine (SVM)**

The dataset contains movie reviews labeled as **positive** or **negative**, and we preprocess the text before training our models.

---

## 📂 Dataset
- **File Name:** `IMDB_Dataset.csv`
- **Columns:**
  - `review`: The text of the movie review.
  - `sentiment`: The sentiment label (positive/negative).

Download the dataset from Kaggle or any other trusted source and place it in your project directory.

---

## 🔧 Installation

Make sure you have **Python 3.x** installed.  
Install the required dependencies using:

```bash
pip install pandas scikit-learn nltk matplotlib seaborn
